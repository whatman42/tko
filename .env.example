# Local development only. Never commit real values.
# Production: use OS secret store / CI secret injection (C1 policy).

TOKOCRYPTO_API_KEY=
TOKOCRYPTO_API_SECRET=

# Optional
# TOKOCRYPTO_RECV_WINDOW=5000
